plugins {
	id("org.gradle.idea")
}

idea {
	module {
		def excludes = [
				"temp",
		]
		excludeDirs.addAll(excludes.collect { new File(rootDir, it) })
	}
}

tasks.named("updateDaemonJvm") { UpdateDaemonJvm task ->
	task.languageVersion = libs.versions.java.compile.map(JavaLanguageVersion::of)
}
